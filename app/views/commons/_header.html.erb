<div class="bg-[#F0F0F0] p-3 flex items-center justify-between">
  <div>
    <h3 class="font-bold text-2xl notranslate hover:underline" translate="no">
       <%= link_to "Højest", root_path%>
    </h3>
  </div>
  <div>
    <% unless current_user.nil? %>
      <%= link_to new_post_path, class: 'hover:text-gray-500' do %>Write Article<% end %>
    <% end %>
  </div>
  <div class="pr-5 flex">
    <div class="mr-7">
      <% if current_user.nil? %>
	      <%= link_to new_user_session_path, class: 'hover:text-gray-500' do %>Sign in<% end %>
      <% else %>
	      <%= form_tag destroy_user_session_path, method: :delete do |f| %>
          <%= submit_tag "Log out", class: "hover:text-gray-500 hover:cursor-pointer" %>
        <% end %>
      <% end %>
    </div>
    <div>
      <%= link_to "How We Work", "https://hojest.com/", target: "_blank", class: "bg-[#FF5633] text-white font-semibold rounded-xl p-3 cursor-pointer  hover:bg-[#FF775A]" %>
    </div>
  </div>
</div>
